# UI 重設計：Focused Planning Hub

## Context

目前的雙面板佈局（左歷史 + 右當期，共 8 個面板）過於擁擠。使用者希望聚焦於「當下的」計畫，歷史資料僅在需要比較時才調出。此次重設計將從「並排展示」轉為「聚焦堆疊 + 按需比較」的模式。

## 設計核心理念

- **當下優先**：全寬單欄顯示當期的日/週/月/年計畫
- **按需歷史**：點擊「比較」按鈕才滑出歷史抽屜
- **漸進式聚焦**：日計畫預設展開 → 點擊展開其他計畫 → 雙擊進入全螢幕專注模式

## 新佈局架構

```
┌──────────────────────────────────────────────┐
│  📅 2026 / 2月 / 第7週 / 2/14(六)  [📅][⚙️][🌙] │  ← 麵包屑導航 + 精簡控制列
├──────────────────────────────────────────────┤
│  ┌──────────────────────────────────────┐    │
│  │ ● 今日計畫  2/14         [比較][▼]  │    │  ← 展開狀態（預設）
│  │ ────────────────────────────────     │    │
│  │  [Edit] [Preview]           [Save]  │    │
│  │  # 2026-02-14 日計畫                │    │
│  │  - Task 1                           │    │
│  │  - Task 2                           │    │
│  └──────────────────────────────────────┘    │
│  ┌──────────────────────────────────────┐    │
│  │ ● 本週計畫  02/08~02/14  [比較][>]  │    │  ← 收合狀態（預覽摘要）
│  │   Goal 1 / Goal 2 / ...            │    │
│  └──────────────────────────────────────┘    │
│  ┌──────────────────────────────────────┐    │
│  │ ● 本月計畫  2026-02      [比較][>]  │    │
│  │   Objective 1 / Objective 2 / ...   │    │
│  └──────────────────────────────────────┘    │
│  ┌──────────────────────────────────────┐    │
│  │ ● 年度計畫  2026         [比較][>]  │    │
│  │   Vision 1 / Vision 2 / ...         │    │
│  └──────────────────────────────────────┘    │
└──────────────────────────────────────────────┘
```

**歷史抽屜（點擊「比較」時從右滑入）：**
```
  主內容（背景變暗）          │ 歷史抽屜 (400px)
                             │ ┌─────────────────────┐
                             │ │ [X]  歷史: 02/13    │
                             │ │ [<前一期] [後一期>]  │
                             │ ├─────────────────────┤
                             │ │ (完整編輯器/預覽)   │
                             │ ├─────────────────────┤
                             │ │ [複製到當期計畫]     │
                             │ └─────────────────────┘
```

## 實作計畫

### Phase 1：基礎架構（HTML + 佈局）

**修改檔案：**
- `frontend/index.html` — 將雙面板結構替換為單欄卡片堆疊 + 歷史抽屜 overlay
- `static/css/main.css` — 新增卡片樣式、抽屜動畫、專注模式樣式；移除雙面板 resize 相關樣式

**新增檔案：**
- `static/js/focused-layout.js` — 取代 `layout-manager.js`，管理卡片堆疊與展開/收合狀態

### Phase 2：PlanCard 元件

**新增檔案：**
- `static/js/components/PlanCard/index.js` — 卡片容器，管理三種狀態（收合/展開/專注）
- `static/js/components/PlanCard/CardHeader.js` — 精簡版 header：狀態點 + 標題 + 比較按鈕 + 展開切換
- `static/js/components/PlanCard/CardPreview.js` — 收合時顯示 markdown 內容的純文字摘要（~120 字）

**修改檔案：**
- `static/js/app.js` — 移除 `loadHistoryPlans()` 呼叫；用 `FocusedLayout` 取代 `LayoutManager`；panels 結構從 `{history, current}` 改為 `{cards: {}}`

**複用檔案（不改動）：**
- `static/js/components/PlanPanel/PanelContent.js` — 卡片展開時的編輯器/預覽直接複用
- `static/js/api.js` — API 完全不變

### Phase 3：歷史抽屜

**新增檔案：**
- `static/js/components/HistoryDrawer/index.js` — 滑入式抽屜容器，內嵌一個 PlanPanel 實例
- `static/js/components/HistoryDrawer/DrawerNavigation.js` — 抽屜內的前/後期導航 + 複製功能

**修改檔案：**
- `frontend/index.html` — 新增抽屜 HTML 結構（backdrop + drawer container）
- `static/js/app.js` — 新增 `openHistoryDrawer(planType)` / `closeHistoryDrawer()` 方法

### Phase 4：專注模式 + 麵包屑 + 快捷鍵 + 動畫潤飾

**修改檔案：**
- `static/js/components/PlanCard/index.js` — 全螢幕專注模式（複用 MaximizeManager 邏輯）
- `frontend/index.html` — header 改為麵包屑導航
- `static/js/app.js` — 新增鍵盤快捷鍵（1-4 跳轉卡片、Escape 退出）
- `static/css/main.css` — 卡片展開/收合/抽屜滑入動畫

### Phase 5：設定遷移 + 佈局切換 + 行動裝置優化

**修改檔案：**
- `static/js/settings-manager.js` — 新增 `layout` 設定（`focused` / `classic`）；新增 `cardOrder` 陣列設定
- `static/js/settings-modal.js` — 新增「佈局」tab，提供聚焦/經典切換；新增卡片排序拖曳 UI
- `static/js/app.js` — 根據 `layout` 設定決定載入 `FocusedLayout` 或 `LayoutManager`
- `static/css/main.css` — 行動裝置斷點：抽屜全寬、麵包屑精簡；拖曳排序樣式

## 不需修改的檔案

- **所有後端檔案** — API 完全不變
- `static/js/api.js` — API client 不變
- `static/js/components/PlanPanel/` — 整包複用於 HistoryDrawer 內部
- `static/js/calendar-modal.js`、`static/js/about-modal.js` — 保持原樣

## 保留但非新佈局必須的檔案

- `static/js/layout-manager.js` — 經典佈局仍需要，保留不刪除
- `static/js/components/PlanPanel/ResizeHandle.js` — 經典佈局的水平 resize 仍需要

## 關鍵技術決策

1. **非手風琴模式**：允許多張卡片同時展開，不強制互斥
2. **使用者自訂排序**：卡片順序可在設定中拖曳調整，預設為 日→週→月→年
3. **歷史抽屜而非分割畫面**：覆蓋式抽屜不佔主內容空間
4. **惰性載入歷史**：僅在開啟抽屜時才載入歷史計畫資料
5. **漸進式寬度**：卡片堆疊設 `max-w-4xl mx-auto`，大螢幕居中不過寬
6. **保留舊佈局**：在設定中提供「聚焦佈局」與「經典雙面板佈局」切換，舊的 `layout-manager.js` 和相關邏輯保留不刪除

## 驗證方式

1. 啟動 FastAPI 後端 + 開啟前端頁面
2. 驗證 4 張當期卡片正確載入（日計畫預設展開）
3. 點擊卡片展開/收合切換正常
4. 點擊「比較」按鈕，歷史抽屜從右滑入，顯示前一期計畫
5. 在抽屜內導航前/後期、複製內容到當期
6. 進入/退出專注模式
7. 日期選擇器切換日期後所有卡片更新
8. 測試行動裝置寬度下的響應式佈局
9. 驗證設定遷移（舊格式 → 新格式）
10. 切換「聚焦佈局」與「經典佈局」功能正常
